services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: pfm
      POSTGRES_PASSWORD: pfm_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
